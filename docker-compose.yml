services:
  queryengine:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      CORS_ORIGIN: "*"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
